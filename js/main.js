$(document).ready( () => {

	// Global, useful DOM elements
	var html = $("html");
	var body = $("body");
	var header = $("#header");
	var footer = $("#footer");
	var adminBar = $("#wpadminbar");
	var adminBarVisible = (adminBar.length > 0);
	
});